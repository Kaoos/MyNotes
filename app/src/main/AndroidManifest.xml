<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ruben.mynotes" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MyNotes"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddNotes"
            android:label="@string/title_activity_add_notes"
            android:parentActivityName=".MyNotes" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.ruben.mynotes.MyNotes" />
        </activity>
        <activity
            android:name=".listElement"
            android:label="@string/title_activity_list_element" >
        </activity>
    </application>

</manifest>
